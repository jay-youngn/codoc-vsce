# Changelog

## 0.0.4

稳定性收尾版本，保留结构化注释浏览与导出能力。

- 修复大文件尾部漏项、批量解析残留、主编号被关联需求覆盖、全部类型展开和源码行号偏移；补齐 C++ 扫描。
- Markdown 保留中文字段、自由正文、嵌套列表、代码块和原有链接；移除硬编码平台链接，导出使用可另存的未命名文档。
- Git 导出新增命令面板入口，使用无 shell 的参数调用、提交校验和 NUL 文件列表，取消立即结束，默认基准改为 `HEAD`。
- 缓存迁入扩展专用工作区存储并校验版本与来源；明确历史／待刷新状态，导出前重新扫描已保存内容。取消和读取失败保留上次成功结果。
- 支持多目录工作区与空工作区命令提示；清理事件、取消订阅、子进程及异步错误，移除手动 GC 和后台定时器。
- Better Comments Next 改为可选集成。新增显式应用／更新／移除命令，支持工作区和用户作用域，启动与升级不再改写全局设置。
- esbuild 替换 webpack；独立类型检查、基础 ESLint 检查和回归测试可重复执行。发布使用白名单，自动生成第三方许可证，不携带外部 npm 依赖或 source map。
- 增加实际 VSIX 内容校验，以及当前稳定版的隔离安装／宿主测试入口。支持基线更新为 VS Code 1.137 / Node.js 24，不再维护旧版兼容。

升级提示：需要 VS Code 1.137 或更新版本。旧仓库缓存不会被自动删除；旧版全局高亮可通过「配置注释高亮 → 移除 → 用户设置」清理。Git 默认范围从隐式 `HEAD~10` 改为明确 `HEAD`。
